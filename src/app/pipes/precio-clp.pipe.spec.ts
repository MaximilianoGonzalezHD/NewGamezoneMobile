import { PrecioClpPipe } from './precio-clp.pipe';

describe('PrecioClpPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecioClpPipe();
    expect(pipe).toBeTruthy();
  });
});
